PyQt5==5.15.10
# macOS: 0.10.35 porque la 1.0.1 aborta con CPU y con GPU pierde memoria en cada cuadro.
# Resto: 1.0.1, la única con paquete para Linux arm64 (Raspberry Pi). Mac Intel: sin paquete;
# la app arranca sin la cámara.
mediapipe==0.10.35; sys_platform == "darwin" and platform_machine == "arm64"
mediapipe==1.0.1; sys_platform != "darwin"
pyttsx3==2.99
pyserial==3.5
